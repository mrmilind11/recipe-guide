import { BlankDishImageDirective } from './blank-dish-image.directive';

describe('BlankDishImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankDishImageDirective();
    expect(directive).toBeTruthy();
  });
});
